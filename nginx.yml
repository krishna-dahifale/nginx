- name: install nginx
  hosts: test
  user: acm
  become: yes
  tasks:  
  - name: update the repo
    apt:
      update_cache: yes

  - name: installing nginx
    apt:
      name: nginx
      state: present
